/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname:"saarthack.github.io"
            }
        ]
    }
}

module.exports = nextConfig
